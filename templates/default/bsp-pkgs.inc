IMAGE_INSTALL += " alsa-utils"
IMAGE_INSTALL += " can-utils"
IMAGE_INSTALL += " i2c-tools"
IMAGE_INSTALL += " irqbalance"
IMAGE_INSTALL += " libsocketcan"
IMAGE_INSTALL += " mtdev"
IMAGE_INSTALL += " pm-utils"
IMAGE_INSTALL += " ${@bb.utils.contains('DISTRO_FEATURES', 'x11 opengl', 'mesa-demos', '', d)}"
